/**
 * Service des messages
 * Contient la logique métier liée à la messagerie
 */

const prisma = require('../config/database');

// TODO: Implémenter findByGame(gameId, cursor, limit)
// TODO: Implémenter create(senderId, gameId, content)
// TODO: Implémenter delete(id)
// TODO: Implémenter findById(id)

module.exports = {
  // TODO: Exporter les fonctions implémentées
};
